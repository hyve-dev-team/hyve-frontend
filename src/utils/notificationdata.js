// Real notification calls against the live backend now handle notifications.
// Kept empty so no demo or mock notifications are displayed.
const notificationData = [];

export default notificationData;
